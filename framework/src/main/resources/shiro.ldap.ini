# =======================
# Shiro INI configuration
# =======================

[main]

# for conf details see: http://shiro.apache.org/session-management.html
# before enabling this make sure shiro-ehcache is included in framework/build.gradle

# NOTE: no credentialsMatcher set here, configured in Moqui conf file (moqui-conf.user-facade.password.@encrypt-hash-type)

ldapRealm = org.moqui.impl.util.MoquiLdapRealm

ldapRealm.contextFactory.url=ldap://ldap.aston.local:389
ldapRealm.contextFactory.systemUsername=uid=proxy_cml,ou=system,dc=aston,dc=sk
ldapRealm.contextFactory.systemPassword=3bfbSW8KbqArb

ldapRealm.userDnTemplate=CN={0},OU=users,OU=astonitm,DC=aston,DC=sk
ldapRealm.ldapSearchBaseQueryFilter=DC=aston,DC=sk,ou=users
ldapRealm.ldapSearchUserQueryFilter=DC=aston,DC=sk

securityManager.realms = $ldapRealm